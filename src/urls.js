const urls = {
  base: '/',
  login: '/login',
  register: '/register',
  dashboard: '/dashboard',
  fileDetails: '/fileDetails/:fileId',
  uploadedFileDetails: '/uploadedFileDetails',
};

export default urls;
